# React Native's Metro bundler and many RN packages assume an npm-style flat
# node_modules and cannot follow pnpm's strict symlinked layout — transitive
# deps such as whatwg-fetch fail to resolve from inside @expo/metro-runtime.
# Hoisting is the layout Expo documents for pnpm monorepos.
node-linker=hoisted
